This program has a parent and a child which take turns reading a number from a file, incrementing that number by 1, and then writing the new number back to the file. The program exits when the number reaches 50.
